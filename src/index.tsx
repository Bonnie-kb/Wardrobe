import React from "react";
import ReactDOM from "react-dom";
import WardrobePlanner from "./App";

ReactDOM.render(
  <React.StrictMode>
    <WardrobePlanner />
  </React.StrictMode>,
  document.getElementById("root")
);
